import { DialogueNode } from '.';

const ropeladder: DialogueNode = {
  index: 781,
  text: ['Why not get a longer ladder?', 'The ones before you must have enjoyed platforming.'],
};

export default [ropeladder];
