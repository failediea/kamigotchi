import { DialogueNode } from '.';

const crystal: DialogueNode = {
  index: 801,
  text: ['The crystal emanates a mellow warmth.', 'The roots wrapped around it are thriving.'],
};

export default [crystal];
