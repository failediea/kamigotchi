import { DialogueNode } from '.';

const scraps: DialogueNode = {
  index: 811,
  text: ['Most scraps are too small or damaged to make anything out.'],
};

const flowermural: DialogueNode = {
  index: 812,
  text: [
    'The mural depicts a dark flower emerging from a seed.',
    'Painted with charcoal from burned books, it might be interpreted as a message of rebirth.',
  ],
};

export default [scraps, flowermural];
