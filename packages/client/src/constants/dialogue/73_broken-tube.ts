import { DialogueNode } from '.';

const largepanel: DialogueNode = {
  index: 731,
  text: ['Faded text is burned into the screen in an unfamiliar language.'],
};

const brokentube: DialogueNode = {
  index: 732,
  text: ['You can go down through this tube to reach a room below.'],
};

export default [largepanel, brokentube];
