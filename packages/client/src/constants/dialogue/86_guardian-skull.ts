import { DialogueNode } from '.';

const ribcagepit: DialogueNode = {
  index: 861,
  text: [
    'It seems that the spine is meant to be used as a ladder.',
    'This temple’s architecture doesn’t match anything else in the cave.',
  ],
};

const giantskull: DialogueNode = {
  index: 862,
  text: ['Probably artificial, like from a Halloween store.'],
};

export default [ribcagepit, giantskull];
