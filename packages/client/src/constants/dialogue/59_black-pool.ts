import { DialogueNode } from '.';

const blackPool: DialogueNode = {
  index: 591,
  text: [
    'A pool of black ooze in the junkyard. The trash has been cleared away to reveal a flagstone path.',
  ],
};

export default [blackPool];
