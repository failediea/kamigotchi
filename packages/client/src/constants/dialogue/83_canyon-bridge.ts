import { DialogueNode } from '.';

const bridge: DialogueNode = {
  index: 831,
  text: [
    `This looks like wood, but it's made of the same composite polymer as the ladders.`,
    'It might be repurposed office furniture.',
  ],
};

export default [bridge];
