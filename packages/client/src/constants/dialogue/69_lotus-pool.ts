import { DialogueNode } from '.';

const lotus: DialogueNode = {
  index: 691,
  text: [
    'Disturbing this flower might be a bad idea.',
    `I won't let you put it into your inventory.`,
  ],
};

export default [lotus];
