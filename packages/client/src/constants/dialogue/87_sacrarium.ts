import { DialogueNode } from '.';

const blackpool: DialogueNode = {
  index: 871,
  text: ['This pool of black ooze seems to mark the lowest point of the caves.'],
};

const pillars: DialogueNode = {
  index: 872,
  text: ['Ancient Greek in style and also age.', 'They must be imported.'],
};

export default [blackpool, pillars];
