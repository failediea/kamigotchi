import { DialogueNode } from '.';

const bluelantern: DialogueNode = {
  index: 821,
  text: ['Something about the lantern’s light sends a shiver across your body.'],
};

export default [bluelantern];
