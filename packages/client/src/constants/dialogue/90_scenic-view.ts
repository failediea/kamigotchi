import { DialogueNode } from '.';

const dragons: DialogueNode = {
  index: 901,
  text: [
    'The dragons look as small as birds down there.',
    'The head mounted on the wall might be from a young one.',
  ],
};

const pipe: DialogueNode = {
  index: 902,
  text: ['Still warm and aromatic.', 'As if it had been smoked recently.'],
};

export default [dragons, pipe];
