import { DialogueNode } from '.';

const bisectedNotebook: DialogueNode = {
  index: 601,
  text: ['Only half-legible. Most of the cut pages have blown away in the breeze.'],
};

const bisectedShovel: DialogueNode = {
  index: 602,
  text: [
    'A shovel cut in half with laser precision.',
    "Even where the steel and wood join, it's a perfectly clean cut.",
  ],
};

export default [bisectedNotebook, bisectedShovel];
