# SECRETS — never commit. pods.json holds raw operator private keys; the
# kamibots-credentials*.json files hold API keys. Runtime state/logs too.
.env
pods.json
kamibots-credentials*.json
ops-state.json
ops-bot.log
THEFT-ALARM.txt
walk*.json
node_modules/
